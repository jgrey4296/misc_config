# -*- mode:cargo-makefile -*-
##-- setup
[env]

[config]
# load_script = ""
# on_error_task = ""

##-- end setup

##-- default
[tasks.default]
alias = "basic"

[tasks.init]
description = "By default this task is invoked at the start of every cargo-make run."
category = "Hooks"

[tasks.end]
description = "By default this task is invoked at the end of every cargo-make run."
category = "Hooks"

# built-in tasks include:
# build, test
# build-file-increment
# dev-test-flow, build-flow
# clean, format
# docs

# run with --list-all-steps
# check with --print-steps
##-- end default

##-- tasks
[tasks.basic]
command = "echo"
args    = ["basic"]
##-- end tasks
