;;; ivy.el -*- lexical-binding: t; -*-

;;;###autoload
(defun +jg-bindings-ivy-finder ()
  (interactive)
  ;; find all +bindings.el in doom-user-dir
  ;; make into (group module file) triples
  ;; provide in ivy
  ;; find selected

  )
