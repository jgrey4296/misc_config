;;; +epub-metadata.el -*- lexical-binding: t; -*-

(defun +jg-org-epub-metadata-gen ()
  ;; Gen content.opf
  ;; insert stylesheet.css
  ;; gen titlepage.xhtml
  ;; generate toc.ncx
  )

(defun +jg-org-epub-create ()
  ;; Check contents
  ;; Zip directory
  ;; Convert to epub
  ;; Move to output location
  ;; remove zip
  )
