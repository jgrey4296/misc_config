# -*- mode: snippet -*-
# name  : install
# uuid  : install
# key   : install
# group : rust, cargo-make
# --
install_crate = { crate_name = "$1", min_version = "${2:0.0.1}" }
# install_command = ""
# install_script = ""