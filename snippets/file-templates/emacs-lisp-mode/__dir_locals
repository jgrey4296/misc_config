;; Directory locals file
;; a List of form:
;; (mode . (list (varname . value)))

((${1:mode-name}. ((${2:var-name} . ${3:value})
                  ;;(eval . (a_func))
                  )
 )
;; (python-mode . ((eval . (+jg-python-activate-venv-and-conda))))
)
$0