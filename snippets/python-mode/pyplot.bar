# -*- mode: snippet -*-
# name: pyplot.bar
# key: pyplot.bar
# --
import matplotlib.pyplot as plt

dict_to_show = {}
barPositions = np.arange(len(dict_to_show))
axes.bar(barPositions,dict_to_show.values(),align='center')
axes.set_xticks(barPositions)
axes.set_xticklabels(dict_to_show.keys())
