# -*- mode: snippet -*-
# name  : doot.task
# uuid  : doot.task
# key   : doot.task
# group :
# --

import doot
from doot.tasker import DootTasker

class $1(DootTasker):

    def __init__(self, name="$2", dirs=None):
        super().__init__(name, dirs)

    def task_detail(self, task):
        task.update({
            "actions" : [],
        })
        return task