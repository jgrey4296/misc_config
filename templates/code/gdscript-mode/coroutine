# -*- mode: snippet -*-
# name  : coroutine
# uuid  : coroutine
# key   : coroutine
# group : godot
# --
func ${1:funcName}(${2:args}) -> ${3:retType}:
     # pause till `resume` is called on obj:
     yield(${4:yieldVal})
