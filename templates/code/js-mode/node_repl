# -*- mode: snippet -*-
# name: node_repl
# key: node_repl
# --
const repl = require('repl');
const cli = repl.start();
//Add imports, definitions etc here
let test = 5;

//Then add to the cli context:
cli.context.test = test;