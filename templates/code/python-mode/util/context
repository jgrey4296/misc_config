# -*- mode: snippet -*-
# name: util.context
# uuid: util.context
# group: contextual
# --
def __enter__(self):
    #return self
    return


def __exit__(self, exc_type, exc_value, exc_traceback):

   # return False to reraise errors
    return
