# -*- mode: snippet -*-
# name  : slice
# uuid  : slice
# key   : slice
# group :
# --
def build_slice(s, l, toks):
    first  = None
    second = None
    if 'first' in toks:
        first = toks['first']

    if 'second' in toks:
        second = toks['second']

    return slice(first, second)
