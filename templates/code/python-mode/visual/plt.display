# -*- mode: snippet -*-
# name: display
# uuid: display
# group: plt
# --
print(f"Interactive: {plt.isinteractive()}")
# For Repl Use:
plt.ion()
# plt.ioff()
# Style Control:
plt.style.available
plt.style.use('ggplot')
# Redraw current figure
plt.draw()
# Close the current figure
plt.close()
# For non-interactive rendering:
plt.show()
# Saving
plt.savefig(expanduser(${1:"~/desktop/test_fig.png"}))
# Get Current figure, axes, figuremanager
plt.gcf()
plt.gca()
plt.get_current_fig_manager()
