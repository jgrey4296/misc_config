# -*- mode: snippet -*-
# name  : display
# uuid  : display
# key   : display
# group :
# --
use std::fmt;

impl fmt::Display for ${1:type} {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "$0")
    }
}
