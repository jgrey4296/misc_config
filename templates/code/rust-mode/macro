# -*- mode: snippet -*-
# name  : macro
# uuid  : macro
# key   : macro
# group :
# --
// Declarative
// https://doc.rust-lang.org/stable/reference/macros-by-example.html
// $ in a macro declars a macro variable
// macro fragment types: block | expr | ident | item | lifetime | literal | meta | pat | pat_param | path | stmt | tt | ty | vis
// apply #[macro_use] to extern crates/modules to import macros
// or export the macro with #[macro_export] on the macro
macro_rules! ${1:name} {
             ($2) => ($3);

}
