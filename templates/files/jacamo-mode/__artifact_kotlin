package ${1:package};

import jason.asSyntax.Atom;
import cartago.*;

// @OPERATION                         : Atomic actions the simulation can trigger
// @LINK                              : Actions for linked artifact execution
// Signals                            : Observable events generated by use.
// execLinkedOp(port, opName, opArgs) : Linked artifact execution

// OpFeedbackParam<type>              : Operation variable for passing information back to agent.
// defineObsProp("name", val);        : Agent Observable Properties.
// getObsProperty("name")             :
// getCurrentOpAgentId()              : artifact access of agent info


public class ${2:name} extends Artifact {
    // Artifact template for $2

    private static final String artifactPort = "artifactPort"

    @OPERATION public void init () {
        // definePort(artifactPort);
        }

    @LINK


}
