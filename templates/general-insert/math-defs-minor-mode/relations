Relation Property Definitions:

X in f: X → Y.                                                                                                                 # domain
Y in f: X → Y.                                                                                                                 # codomain

∀x,z ∈ X, ∀y ∈ Y : xRy ∧ zRy ⇒ x = z                                                                                           # Injective. left-unique.
∀y ∈ Y,   ∃x ∈ X : xRy                                                                                                         # Surjective. right-total.
∀x ∈ X,   ∃y ∈ Y : f(x) = y                                                                                                    # Total. left-total.
∀x ∈ X, ∀y,z ∈ Y : xRy ∧ xRz ⇒ y = z                                                                                           # Partial. right-unique.
injective and surjective                                                                                                       # Bijective

X, Y and R ⊆ X ⨯ Y : (R^T) = y(R^T)x ⇔ xRy.                                                                                    # transpose
f(f(x)) = x.                                                                                                                   # involution

∀x ∈ X : xRx.                                                                                                                  # Reflexive
∀x ∈ X : not xRx.                                                                                                              # Irreflexive
∀x,y ∈ X : xRy ⇒ yRx.                                                                                                          # Symmetric
∀x,y ∈ X : xRy ⇒ not yRx.                                                                                                      # Asymmetric
∀x,y ∈ X : xRy and yRx ⇒ x = y.                                                                                                # Antisymmetric
∀x,y,z ∈ X : xRy and yRz ⇒ xRz.                                                                                                # Transitive
∀x,y ∈ X : x ≠ y ⇒ xRy or yRx.                                                                                                 # Connected
∀x,y ∈ X : xRy or yRx.                                                                                                         # Strongly Connected

∀x,y,z ∈ S: (xRy)Rz = xR(yRz).                                                                                                 # Associativity
∀x,y ∈ S  : xRy = yRx.                                                                                                         # Commutativity
∀x,y,z ∈ S : xR(yR'z) = (xRy)R'(xRz).   eg: Multiplication, ∧, ∨.                                                              # Distributivity
f(x) = f(f(x)) = f(...f(x)). eg: multiplication by 1.                                                                          # Idempotence
xR(xR'y) = xR'(xRy) = x.                                                                                                       # Absorption
dual((P, <)) = (P, ⧼), ∀x,y ∈ P : x < y ⇔ y ⧼ x.                                                                               # Duality

relations A, B : A\B      = complement(A^T complement(B))                                                                      # Left Residual
relations A, B: A/B       = complement(complement(A) B^T))                                                                     # Right Residual
relations A, B: syq(A, B) = complement(A^T complement(F)) ∩ complement(complement(E)^T F)                                      # Symmetric Quotient


reflexive, transitive relation.                                                                                                # Pre-Order
reflexive, symmetric, transitive relation.                                                                                     # Equivalence Relation

reflexive,   antisymmetric, transitive relation.               Not every element is comparable.                                # Partial Order
reflexive,   antisymmetric, transitive and connected relation. Every element is comparable.                                    # Total Order
a set S, relation R : (S, R).                                                                                                  # Poset. Partial Ordered Set.
interval I of poset (P, <) : I ⊂ P,  ∀x,y ∈ I, ∀z ∈ P : x < z < y ⇒ z ∈ Y.                                                     # Interval

irreflexive, antisymmetric, transitive relation.                                                                               # Strict Partial Order
irreflexive, antisymmetric, transitive and connected relation.                                                                 # Strict Total Order

smallest reflexive relation over X containing R.                                                                               # Reflexive Closure
smallest transitive relation over X containing R.                                                                              # Transitive Closure
smallest equivalent relation over X containing R.                                                                              # Equivalence Closure

For posets (S, <), (T, ⧼), function f(S → T) if ∀x,y ∈ S, x < y ⇒ f(x) ⧼ f(y).                                                 # order-preserving. (*monotonic*)
. For posets (S, <), (T, ⧼), functiion f(S → T) if ∀x,y ∈ S, x < y ⇒ f(y) ⧼ f(x).                                              # order-reversing. (*antitonic*)
For posets (S, <), (T, ⧼), function f(S → T) if ∀x, y ∈ S: f(x) ⧼ f(y) ⇒ x < y.                                                # order-reflecting
both order-preserving and order-reflecting.                                                                                    # order-embedding

inf(S) ∈ poset (P, <), S ⊂ P, ∀x ∈ S : inf(S) < x.                                                                             # infimum. Meet. Greatest Lower Bound. *Single Element or nothing*.
sup(S) ∈ poset (P, <), S ⊂ P, ∀x ∈ S : x < sup(S).                                                                             # supremum. Join. Least Upper Bound. *Single Element or nothing*.

Poset X, ∀Y ⊂ X : inf(Y) != ∅                                                                                                  # Semi-Lattice.
Poset with infimum and supremum.                                                                                               # Lattice
